<VirtualHost *:80>
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        RewriteEngine On

        # 1. Si el archivo o carpeta existe, servirlo
        RewriteCond %{REQUEST_FILENAME} -f [OR]
        RewriteCond %{REQUEST_FILENAME} -d
        RewriteRule ^ - [L]

        # 2. Si empieza por /api, enviar a Symfony (index.php)
        RewriteCond %{REQUEST_URI} ^/api
        RewriteRule ^ /index.php [L]

        # 3. Si es la raíz "/", redirigir al landing
        #RewriteCond %{REQUEST_URI} ^/$
        #RewriteRule ^ /landing/ [R=302,L]

        # 4. Si empieza por /landing y no existe archivo, servir Angular
        RewriteCond %{REQUEST_URI} ^/landing
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule ^ /landing/index.html [L]

        # 5. El resto, que lo resuelva Symfony
        RewriteRule ^ /index.php [L]
    </Directory>

    <Directory /var/www/html/public/landing>
        Options -Indexes +FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
