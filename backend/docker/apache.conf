<VirtualHost *:80>
    DocumentRoot /var/www/html/public

    # Symfony (API)
    <Directory /var/www/html/public>
        AllowOverride All
        Require all granted
        Options -Indexes +FollowSymLinks

        RewriteEngine On

        # Permitir acceso a archivos reales
        RewriteCond %{REQUEST_FILENAME} -f [OR]
        RewriteCond %{REQUEST_FILENAME} -d
        RewriteRule ^ - [L]

        # Redirigir raíz "/" a Angular
        RewriteCond %{REQUEST_URI} ^/$
        RewriteRule ^ /landing/ [R=302,L]

        # Redirigir rutas de Angular no existentes a su index.html
        RewriteCond %{REQUEST_URI} ^/landing
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule ^ /landing/index.html [L]

        # Todas las demás rutas las gestiona Symfony (API)
        RewriteRule ^ /index.php [L]
    </Directory>

    # Servir directamente archivos estáticos desde /landing
    Alias /landing /var/www/html/public/landing
    <Directory /var/www/html/public/landing>
        Options -Indexes +FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
